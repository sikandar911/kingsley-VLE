# Build Configuration for Docker Versioning
# Update VERSION for each release
VERSION=1.0.0

# Build metadata (auto-populated or manual)
BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
VCS_REF=$(git rev-parse --short HEAD)
