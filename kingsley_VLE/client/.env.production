# Production Frontend Environment
# Runtime config should provide the production API URL.
# Keep this only as a fallback if /api/config is unavailable.
VITE_API_URL=/api
# VITE_API_URL=http://localhost:5000/api